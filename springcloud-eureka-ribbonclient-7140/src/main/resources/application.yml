server:
  port: 7140

spring:
  application:
    name: springcloud-eureka-client-ribbon

eureka:
  instance:
    instance-id: ${spring.application.name}
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7100/eureka/ #Eureka地址


